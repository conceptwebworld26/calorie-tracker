# Dependencies
node_modules/

# Environment / secrets — never commit real keys
.env
.env.local
.env.*.local

# Build output
.next/
dist/
build/
out/
*.tsbuildinfo

# Runtime data (SQLite database is created on first run)
data/
*.db
*.db-shm
*.db-wal

# OS / editor
.DS_Store
Thumbs.db
*.log

# Playwright MCP verification output
.playwright-mcp/

# Machine-local skill symlinks (the real files live in .agents/)
.claude/
